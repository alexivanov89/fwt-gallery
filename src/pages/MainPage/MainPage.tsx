import { CardList } from '../../components/CardList';
import { Layout } from '../../components/Layout';

const MainPage = () => {
  return (
    <Layout>
      <CardList />
    </Layout>
  );
};

export default MainPage;
